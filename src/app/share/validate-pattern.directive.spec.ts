import { ValidatePatternDirective } from './validate-pattern.directive';

describe('ValidatePatternDirective', () => {
  it('should create an instance', () => {
    const directive = new ValidatePatternDirective();
    expect(directive).toBeTruthy();
  });
});
